import { StyleSheet } from "react-native";

const estilos = StyleSheet.create({
      imagemFundo: {
            flex: 1,
            resizeMode: "cover",
            width: "auto",
            height: "auto",
          }
});

export default estilos;